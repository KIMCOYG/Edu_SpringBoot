spring:
#  datasource:
#    url: jdbc:h2:mem:test #메모리 디비 사용
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop # 스프링부트는 create가 아니라 create-drop으로 돌아감
#    properties:
#      hibernate:
#        #        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug # hibernate가 생성하는 sql이 다 보임
    org.hibernate.type: trace
